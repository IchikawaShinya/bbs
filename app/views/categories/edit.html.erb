<h1>Editing Category</h1>

<p id="notice"><%= notice %></p>
<div>
<%= form_for("categories", :url => {:action => 'update'}) do |f| %>
  <div class="actions">
  	<table class="category">
  		<tr>
  			<th><%= t("attributes.category_name") %></th>
  			<th><%= t("attributes.order_num") %></th>
  			<th></th>
  		</tr>
	    <% @categories.each_with_index do |category,index| %>
	    	<% name = "category["+index.to_s+"]" %>
	      <tr>
	  		<td>
	  			<%= hidden_field name, "id" , :value => category["id"].to_i %>
	  			<%= text_field name, "category_name" , :value => category["category_name"] %></td>
	  		<td><%= text_field name, "order_num" , :value => category["order_num"] %></td>
	        <td><%= link_to t("attributes.delete"), category, method: :delete, data: { confirm: t("attributes.delete_confirm") } %></td>
	      </tr>
	    <% end %>
  	</table>
  	
    <%= f.submit %>
<% end %>
</div>

<%= link_to t("attributes.attributes.back"), categories_path %>
